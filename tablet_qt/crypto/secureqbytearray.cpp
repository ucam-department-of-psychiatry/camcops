/*

#include "secureqbytearray.h"


SecureQByteArray::SecureQByteArray()
{
}


SecureQByteArray::SecureQByteArray(const char* str) :
    QByteArray(str)
{
}


SecureQByteArray::SecureQByteArray(const char* data, int size) :
    QByteArray(data, size)
{
}


SecureQByteArray::SecureQByteArray(int size, char ch) :
    QByteArray(size, ch)
{
}


SecureQByteArray::SecureQByteArray(const QByteArray& other) :
    QByteArray(other)
{
}


SecureQByteArray::SecureQByteArray(const SecureQByteArray& other) :
    QByteArray(other)
{
}


SecureQByteArray::~SecureQByteArray()
{
    // Wipe it with zeros.
    for (int i = 0; i < length(); ++i) {
        (*this)[i] = 0;
    }
}

*/
